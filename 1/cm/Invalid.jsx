import React,{useEffect}from"react";import{FADE_IN_STRONG_CLASS_NAME,FLEX_CENTERED_CLASS_NAME,PREFIX}from"../cn/classes-names";export default function({fsLightbox:{isl,sawu,smw},i}){useEffect(function(){isl[i]=true;sawu[i]();smw[i].current.classList.add(FADE_IN_STRONG_CLASS_NAME)});return<div className={`${PREFIX}invalid-file-wrapper ${FLEX_CENTERED_CLASS_NAME}`}>Invalid source</div>}
